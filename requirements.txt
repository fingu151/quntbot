# =============================================================================
# quntbot dependencies
# Install: pip install -r requirements.txt
# (Comments are ASCII-only to avoid Windows cp949 decoding errors)
# =============================================================================

# --- Data collection (Phase 1) ---
pykrx==1.2.7                  # KRX data: tickers, OHLCV, market cap, fundamentals
dart-fss==0.4.3                # OpenDART financial statements for quality metrics
requests==2.32.3               # HTTP client (KIS API)
pypdf==5.1.0                   # PDF text extraction for broker research reports

# --- Data processing (Phase 1, 2, 3) ---
pandas==2.2.3                  # DataFrames (prices, fundamentals, scoring)
numpy==2.1.3                   # Pinned numeric runtime used by pandas
SQLAlchemy==2.0.36             # SQLite ORM

# --- Trading engine (Phase 4) ---
python-dotenv==1.0.1           # Load API keys from .env
APScheduler==3.10.4            # Scheduler (pre-market rebalance)

# --- Notifications + dashboard (Phase 5) ---
streamlit==1.40.1              # Web dashboards

# --- Logging / utils ---
loguru==0.7.2                  # Friendly logging (loguru.logger.info() one-liner)
setuptools==80.9.0             # pykrx runtime dep (pkg_resources)

# --- Tests (dev) ---
pytest==8.3.3                  # Unit tests
