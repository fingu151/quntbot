# 프로젝트 규칙

- 코드 수정 전 관련 파일 최소 5개 이상 읽기
- 추측으로 답하지 말 것 ─ 모르면 Grep/Read로 확인
- 변경 후 반드시 검증 ─ AST 문법 체크, 테스트 실행, 로그 확인 중 하나
- 한 번에 하나씩 ─ 여러 변경 동시 X
- 구현 전에 반드시 Plan 모드로 설계부터
- 데이터 기반 결정 ─ 파라미터 조정 시 DB/로그에서 실제 수치 뽑고 판단
- AI 협업 게으름 방지 (안 읽고 답하지 말 것)

---

# Karpathy 코딩 가이드라인

LLM의 흔한 코딩 실수를 줄이기 위한 행동 지침입니다.

## 1. 코딩 전에 생각하기

구현 전에:
- 가정을 명시적으로 표현하기. 불확실하면 질문하기.
- 여러 해석이 가능하면 모두 제시하고 몰래 선택하지 않기.
- 더 간단한 방법이 있으면 말하기.
- 불명확한 부분이 있으면 멈추고 질문하기.

## 2. 단순함 우선

- 요청받은 것 이상의 기능 추가 금지.
- 단일 사용 코드에 불필요한 추상화 금지.
- 요청하지 않은 "유연성"이나 "설정 가능성" 추가 금지.
- 불가능한 시나리오에 대한 에러 핸들링 금지.
- 200줄로 쓸 수 있는 걸 50줄로 쓸 수 있다면 다시 작성하기.

## 3. 수술적 변경

기존 코드 수정 시:
- 인접한 코드, 주석, 포맷을 임의로 "개선"하지 않기.
- 멀쩡한 코드를 리팩토링하지 않기.
- 내 방식이 달라도 기존 스타일 유지하기.
- 관련 없는 데드 코드를 발견하면 언급만 하고 삭제하지 않기.

변경으로 고아가 된 것들:
- 내 변경으로 인해 불필요해진 import/변수/함수만 제거하기.

## 4. 목표 중심 실행

작업을 검증 가능한 목표로 변환:
- "유효성 검사 추가" → "잘못된 입력에 대한 테스트 작성 후 통과시키기"
- "버그 수정" → "버그를 재현하는 테스트 작성 후 통과시키기"

다단계 작업은 간략한 계획을 먼저 제시:
```
1. [단계] → 검증: [확인]
2. [단계] → 검증: [확인]
```

